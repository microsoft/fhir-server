# Ignixa Shim Register

This register tracks known compatibility shims and SDK fallbacks while Ignixa support is completed. New Firely/Ignixa bridges must be added here or removed before merge readiness.

| ID | Surface | File(s) | Allowed mode | Reason | Severity / priority | Owner / team | Planned closure | Validation |
|---|---|---|---|---|---|---|---|---|
| SHIM-PROJECTION-001 | RawResourceElement `_summary` / `_elements` output projection | `src/Microsoft.Health.Fhir.Shared.Core/Ignixa/IgnixaFhirJsonOutputFormatter.cs` | Hybrid only | Raw persisted resources do not carry Ignixa node backing, so projection still uses Firely when explicitly allowed by the fallback guard. | P1 | FHIR | Add an Ignixa-backed raw projection path or convert raw JSON to `ResourceJsonNode` before projection. | RawResourceElement projection guard tests in Ignixa and Hybrid modes. |
| SHIM-PATCH-001 | FHIRPath PATCH | `src/Microsoft.Health.Fhir.Shared.Core/Features/Resources/Patch/` | Hybrid only | FHIRPath PATCH currently depends on Firely model infrastructure. Strict Ignixa mode rejects this path through the fallback guard until an Ignixa-native PATCH implementation exists. Ignixa.Application prior art has add, replace, delete, move, and insert executors, but it depends on app-layer mutator infrastructure not present here and does not cover upsert. | P0 | FHIR | Implement Ignixa-native PATCH path or an explicitly supported adapter for add, replace, delete, move, insert, and upsert operations. | PATCH unit coverage verifies strict Ignixa mode rejects the guarded Firely path and Hybrid mode preserves the guarded Firely behavior; PATCH E2E coverage remains required when native Ignixa support is added. |
| SHIM-VALIDATION-001 | Resource model attribute validation fallback | `src/Microsoft.Health.Fhir.Shared.Core/Features/Validation/IgnixaResourceValidator.cs` | Hybrid only | Non-Ignixa `ResourceElement` validation and final model attribute validation still use Firely validation behavior when explicitly allowed by the fallback guard. | P0 | FHIR | Replace remaining model attribute validation fallback with Ignixa-native validation or fail fast in Ignixa mode. | Ignixa validator fallback tests in Ignixa and Hybrid modes. |
| SHIM-VALIDATION-002 | Conformance backbone cardinality validation | `src/Microsoft.Health.Fhir.Shared.Core/Features/Validation/IgnixaResourceValidator.cs` | Ignixa only | Ignixa native validation does not yet report required nested/backbone fields for selected conformance resources, so fhir-server applies supplemental cardinality checks in strict Ignixa mode for StructureDefinition differential and snapshot elements, SearchParameter components, ValueSet include/exclude concepts, CodeSystem concepts, and OperationDefinition parameters. Firely and Hybrid modes explicitly use guarded Firely conformance validation instead. Other conformance resource nested cardinality remains covered by Ignixa native validation unless a failing case is identified. | P0 | FHIR | Remove supplemental checks once Ignixa validation enforces these required nested conformance fields directly. | Nested invalid conformance tests in `IgnixaResourceValidatorTests`; Hybrid fallback characterization test. |
| SHIM-SEARCH-001 | Search value conversion | `src/Microsoft.Health.Fhir.Core/Features/Search/Converters/` | Hybrid and Ignixa | Search converter boundary still uses `ITypedElement` compatibility infrastructure. | P1 | FHIR | Add SDK-aware converter seam or approve the adapter as supported infrastructure. | Search indexing/converter tests in both modes. |
