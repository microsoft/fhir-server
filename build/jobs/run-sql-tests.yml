parameters:
- name: version
  type: string
- name: keyVaultName
  type: string
- name: containerAppName
  type: string
- name: integrationSqlServerName
  type: string
- name: mainCategoryFilter
  type: string
  default: 'Category!=ExportLongRunning&Category!=IndexAndReindex&Category!=BulkUpdate'
- name: runReindexJob
  type: boolean
  default: true
- name: runBulkUpdateJob
  type: boolean
  default: true
# When set, emits deployment jobs targeting this ADO Environment so the
# Exclusive Lock check on the env serializes concurrent CI runs.
- name: lockEnvironment
  type: string
  default: ''

jobs:
# ---------- Integration tests ----------
- ${{ if eq(parameters.lockEnvironment, '') }}:
  - job: SqlIntegrationTests
    timeoutInMinutes: 75
    pool:
      name: '$(SharedLinuxPool)'
      vmImage: '$(LinuxVmImage)'
    steps:
    - template: ../tasks/sql-integration-tests-steps.yml
      parameters:
        version: ${{ parameters.version }}
        keyVaultName: ${{ parameters.keyVaultName }}
        integrationSqlServerName: ${{ parameters.integrationSqlServerName }}

- ${{ if ne(parameters.lockEnvironment, '') }}:
  - deployment: SqlIntegrationTests
    timeoutInMinutes: 75
    environment: ${{ parameters.lockEnvironment }}
    pool:
      name: '$(SharedLinuxPool)'
      vmImage: '$(LinuxVmImage)'
    strategy:
      runOnce:
        deploy:
          steps:
          - template: ../tasks/sql-integration-tests-steps.yml
            parameters:
              version: ${{ parameters.version }}
              keyVaultName: ${{ parameters.keyVaultName }}
              integrationSqlServerName: ${{ parameters.integrationSqlServerName }}

# ---------- E2E tests ----------
- ${{ if eq(parameters.lockEnvironment, '') }}:
  - job: 'sqlE2eTests'
    timeoutInMinutes: 120
    dependsOn: []
    pool:
      name: '$(SharedLinuxPool)'
      vmImage: '$(LinuxVmImage)'
    steps:
    - template: e2e-setup.yml
    - template: e2e-tests.yml
      parameters:
        version: ${{ parameters.version }}
        containerAppName: '${{ parameters.containerAppName }}'
        appServiceType: 'SqlServer'
        categoryFilter: '${{ parameters.mainCategoryFilter }}'

- ${{ if ne(parameters.lockEnvironment, '') }}:
  - deployment: 'sqlE2eTests'
    timeoutInMinutes: 120
    dependsOn: []
    environment: ${{ parameters.lockEnvironment }}
    pool:
      name: '$(SharedLinuxPool)'
      vmImage: '$(LinuxVmImage)'
    strategy:
      runOnce:
        deploy:
          steps:
          - template: e2e-setup.yml
          - template: e2e-tests.yml
            parameters:
              version: ${{ parameters.version }}
              containerAppName: '${{ parameters.containerAppName }}'
              appServiceType: 'SqlServer'
              categoryFilter: '${{ parameters.mainCategoryFilter }}'

# ---------- E2E Reindex (conditional) ----------
- ${{ if eq(parameters.runReindexJob, true) }}:
  - ${{ if eq(parameters.lockEnvironment, '') }}:
    - job: 'sqlE2eTests_Reindex'
      timeoutInMinutes: 120
      dependsOn: []
      pool:
        name: '$(SharedLinuxPool)'
        vmImage: '$(LinuxVmImage)'
      steps:
      - template: e2e-setup.yml
      - template: e2e-tests.yml
        parameters:
          version: ${{ parameters.version }}
          containerAppName: '${{ parameters.containerAppName }}'
          appServiceType: 'SqlServer'
          categoryFilter: 'Category=IndexAndReindex'
          testRunTitleSuffix: ' Reindex'

  - ${{ if ne(parameters.lockEnvironment, '') }}:
    - deployment: 'sqlE2eTests_Reindex'
      timeoutInMinutes: 120
      dependsOn: []
      environment: ${{ parameters.lockEnvironment }}
      pool:
        name: '$(SharedLinuxPool)'
        vmImage: '$(LinuxVmImage)'
      strategy:
        runOnce:
          deploy:
            steps:
            - template: e2e-setup.yml
            - template: e2e-tests.yml
              parameters:
                version: ${{ parameters.version }}
                containerAppName: '${{ parameters.containerAppName }}'
                appServiceType: 'SqlServer'
                categoryFilter: 'Category=IndexAndReindex'
                testRunTitleSuffix: ' Reindex'

# ---------- E2E BulkUpdate (conditional) ----------
- ${{ if eq(parameters.runBulkUpdateJob, true) }}:
  - ${{ if eq(parameters.lockEnvironment, '') }}:
    - job: 'sqlE2eTests_BulkUpdate'
      timeoutInMinutes: 120
      dependsOn: []
      pool:
        name: '$(SharedLinuxPool)'
        vmImage: '$(LinuxVmImage)'
      steps:
      - template: e2e-setup.yml
      - template: e2e-tests.yml
        parameters:
          version: ${{ parameters.version }}
          containerAppName: '${{ parameters.containerAppName }}'
          appServiceType: 'SqlServer'
          categoryFilter: 'Category=BulkUpdate'
          testRunTitleSuffix: ' BulkUpdate'

  - ${{ if ne(parameters.lockEnvironment, '') }}:
    - deployment: 'sqlE2eTests_BulkUpdate'
      timeoutInMinutes: 120
      dependsOn: []
      environment: ${{ parameters.lockEnvironment }}
      pool:
        name: '$(SharedLinuxPool)'
        vmImage: '$(LinuxVmImage)'
      strategy:
        runOnce:
          deploy:
            steps:
            - template: e2e-setup.yml
            - template: e2e-tests.yml
              parameters:
                version: ${{ parameters.version }}
                containerAppName: '${{ parameters.containerAppName }}'
                appServiceType: 'SqlServer'
                categoryFilter: 'Category=BulkUpdate'
                testRunTitleSuffix: ' BulkUpdate'
