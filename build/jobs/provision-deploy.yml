parameters:
- name: version
  type: string
- name: webAppName
  type: string
- name: acaEnvironmentName
  type: string
- name: subscription
  type: string
- name: resourceGroup
  type: string
- name: testEnvironmentUrl
  type: string
- name: imageTag
  type: string
- name: dataStore
  type: string
  # Accepted values: 'sql' or 'cosmos'
- name: schemaAutomaticUpdatesEnabled
  type: string
  default: 'auto'
- name: sqlServerName
  type: string
  default: ''
- name: sqlComputeTier
  type: string
  default: ''
- name: reindexEnabled
  type: boolean
  default: true
- name: minReplicas
  type: number
  default: 0
- name: maxReplicas
  type: number
  default: 8
- name: containerCpu
  type: string
  default: '0.5'
- name: containerMemory
  type: string
  default: '1Gi'
- name: cosmosAutoscaleMaxThroughput
  type: number
  default: 10000
- name: sqlElasticPoolName
  type: string
  default: ''
- name: taskHostingPollingFrequencyInSeconds
  type: number
  default: 1
- name: searchParameterCacheRefreshIntervalSeconds
  type: number
  default: 2
- name: systemConformanceProviderRefreshIntervalSeconds
  type: number
  default: 5
- name: keyVaultName
  type: string

jobs:
- job: provisionEnvironment
  pool:
    name: '$(SharedLinuxPool)'
    vmImage: '$(LinuxVmImage)'
  steps:
  - task: AzureKeyVault@1
    displayName: 'Azure Key Vault: resolute-oss-tenant-info'
    inputs:
      azureSubscription: ${{ parameters.subscription }}
      KeyVaultName: 'resolute-oss-tenant-info'

  - task: AzurePowerShell@5
    name: SetAcaOutputs
    displayName: 'Deploy ACA (${{ parameters.dataStore }}) and emit test URL'
    retryCountOnTaskFailure: 1
    inputs:
      azureSubscription: ${{ parameters.subscription }}
      azurePowerShellVersion: latestVersion
      pwsh: true
      ScriptType: inlineScript
      Inline: |
        Add-Type -AssemblyName System.Web

        # Import retry helper for transient error handling
        . $(System.DefaultWorkingDirectory)/release/scripts/PowerShell/Invoke-WithRetry.ps1

        $deployPath = "$(System.DefaultWorkingDirectory)/test/Configuration"

        $testConfig = ConvertFrom-Json (Get-Content -Raw "$deployPath/testconfiguration.json")
        $flattenedTestConfig = $(System.DefaultWorkingDirectory)/release/scripts/PowerShell/ConvertTo-FlattenedConfigurationHashtable.ps1 -InputObject $testConfig

        $additionalProperties = $flattenedTestConfig
        $additionalProperties["TaskHosting__PollingFrequencyInSeconds"] = ${{ parameters.taskHostingPollingFrequencyInSeconds }}
        $additionalProperties["FhirServer__CoreFeatures__SearchParameterCacheRefreshIntervalSeconds"] = ${{ parameters.searchParameterCacheRefreshIntervalSeconds }}
        $additionalProperties["FhirServer__CoreFeatures__SystemConformanceProviderRefreshIntervalSeconds"] = ${{ parameters.systemConformanceProviderRefreshIntervalSeconds }}
        $additionalProperties["ASPNETCORE_FORWARDEDHEADERS_ENABLED"] = "true"

        $dataStore = "${{ parameters.dataStore }}"

        $staticEnvNames = @(
            "ASPNETCORE_FORWARDEDHEADERS_ENABLED",
            "KeyVault__Endpoint",
            "FhirServer__Security__Enabled",
            "FhirServer__Security__EnableAadSmartOnFhirProxy",
            "FhirServer__Security__Authentication__Authority",
            "FhirServer__Security__Authentication__Audience",
            "DataStore",
            "TaskHosting__Enabled",
            "TaskHosting__MaxRunningTaskCount",
            "FhirServer__Operations__Export__Enabled",
            "FhirServer__Operations__Export__StorageAccountUri",
            "FhirServer__Operations__Import__Enabled",
            "FhirServer__Operations__IntegrationDataStore__StorageAccountUri",
            "FhirServer__Operations__ConvertData__Enabled",
            "FhirServer__Operations__ConvertData__ContainerRegistryServers__0",
            "FhirServer__Operations__Reindex__Enabled"
        )

        if ($dataStore -eq 'sql') {
            $additionalProperties["SqlServer__DeleteAllDataOnStartup"] = "false"
            $additionalProperties["SqlServer__AllowDatabaseCreation"] = "true"
            $staticEnvNames += @(
                "SqlServer__Initialize",
                "SqlServer__SchemaOptions__AutomaticUpdatesEnabled",
                "SqlServer__DeleteAllDataOnStartup",
                "SqlServer__AllowDatabaseCreation"
            )
        } else {
            $staticEnvNames += @(
                "CosmosDb__UseManagedIdentity",
                "CosmosDb__ContinuationTokenSizeLimitInKb",
                "CosmosDb__UseQueueClientJobs"
            )
        }

        $additionalEnvVars = @()
        foreach ($entry in $additionalProperties.GetEnumerator()) {
            if ($staticEnvNames -contains $entry.Key) {
                continue
            }

            if ($null -eq $entry.Value) {
                continue
            }

            $value = if ($entry.Value -is [bool]) { $entry.Value.ToString().ToLowerInvariant() } else { [string]$entry.Value }
            if ([string]::IsNullOrWhiteSpace($value)) {
                continue
            }

            $additionalEnvVars += @{
                name = [string]$entry.Key
                value = $value
            }
        }

        $webAppName = "${{ parameters.webAppName }}".ToLowerInvariant()
        $acaEnvironmentName = "${{ parameters.acaEnvironmentName }}".ToLowerInvariant()

        if ($acaEnvironmentName.Length -gt 32) {
            $acaEnvironmentName = $acaEnvironmentName.Substring(0, 32).TrimEnd('-')
        }

        if ($acaEnvironmentName.Length -lt 2) {
            $acaEnvironmentName = "ca-$acaEnvironmentName"
        }

        $resourceGroupName = "${{ parameters.resourceGroup }}"

        # --- Data-store-specific pre-deploy setup ---
        if ($dataStore -eq 'sql') {
            $sqlServerName = "${{ parameters.sqlServerName }}".ToLowerInvariant()
            $sqlDatabaseName = "FHIR${{ parameters.version }}"
            $sqlElasticPoolName = "${{ parameters.sqlElasticPoolName }}"
            $existingDb = Get-AzSqlDatabase -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -DatabaseName $sqlDatabaseName -ErrorAction SilentlyContinue
            if ($null -eq $existingDb) {
                if ([string]::IsNullOrWhiteSpace($sqlElasticPoolName)) {
                    Write-Host "Database '$sqlDatabaseName' does not exist on server '$sqlServerName'. Creating standalone (GP_Gen5_4)..."
                    Invoke-WithRetry -OperationName "Create SQL Database" -ScriptBlock {
                        New-AzSqlDatabase -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -DatabaseName $sqlDatabaseName -Edition "GeneralPurpose" -RequestedServiceObjectiveName "GP_Gen5_4" -CollationName "SQL_Latin1_General_CP1_CI_AS" -ErrorAction Stop
                    }
                } else {
                    Write-Host "Database '$sqlDatabaseName' does not exist on server '$sqlServerName'. Creating in elastic pool '$sqlElasticPoolName'..."
                    Invoke-WithRetry -OperationName "Create SQL Database in Elastic Pool" -ScriptBlock {
                        New-AzSqlDatabase -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -DatabaseName $sqlDatabaseName -ElasticPoolName $sqlElasticPoolName -CollationName "SQL_Latin1_General_CP1_CI_AS" -ErrorAction Stop
                    }
                }
                Write-Host "Database '$sqlDatabaseName' created successfully."
            } else {
                Write-Host "Database '$sqlDatabaseName' already exists on server '$sqlServerName'."
                if (-not [string]::IsNullOrWhiteSpace($sqlElasticPoolName) -and $existingDb.ElasticPoolName -ne $sqlElasticPoolName) {
                    Write-Host "Moving existing database '$sqlDatabaseName' into elastic pool '$sqlElasticPoolName'..."
                    Invoke-WithRetry -OperationName "Move SQL Database to Elastic Pool" -ScriptBlock {
                        Set-AzSqlDatabase -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -DatabaseName $sqlDatabaseName -ElasticPoolName $sqlElasticPoolName -ErrorAction Stop
                    }
                }
            }

            $templateFile = "$(System.DefaultWorkingDirectory)/samples/templates/aca/fhir-sql.bicep"
        } else {
            # Cosmos DB account uses the same name as the container app (matching App Service convention)
            $cosmosDbAccountName = $webAppName

            # Ensure Cosmos DB account exists
            $existingAccount = Get-AzCosmosDBAccount -ResourceGroupName $resourceGroupName -Name $cosmosDbAccountName -ErrorAction SilentlyContinue
            if ($null -eq $existingAccount) {
                Write-Host "Cosmos DB account '$cosmosDbAccountName' does not exist. Creating..."
                Invoke-WithRetry -OperationName "Create Cosmos DB Account" -ScriptBlock {
                    New-AzCosmosDBAccount `
                        -ResourceGroupName $resourceGroupName `
                        -Name $cosmosDbAccountName `
                        -Location (Get-AzResourceGroup -Name $resourceGroupName).Location `
                        -ApiKind "Sql" `
                        -DefaultConsistencyLevel "Strong" `
                        -ErrorAction Stop
                }
                Write-Host "Cosmos DB account '$cosmosDbAccountName' created successfully."
            } else {
                Write-Host "Cosmos DB account '$cosmosDbAccountName' already exists."
            }

            # Ensure Cosmos DB database exists
            $cosmosDbDatabaseName = "health"
            $existingDb = Get-AzCosmosDBSqlDatabase -ResourceGroupName $resourceGroupName -AccountName $cosmosDbAccountName -Name $cosmosDbDatabaseName -ErrorAction SilentlyContinue
            if ($null -eq $existingDb) {
                Write-Host "Cosmos DB database '$cosmosDbDatabaseName' does not exist. Creating with autoscale 10000 RU..."
                Invoke-WithRetry -OperationName "Create Cosmos DB Database" -ScriptBlock {
                    New-AzCosmosDBSqlDatabase `
                        -ResourceGroupName $resourceGroupName `
                        -AccountName $cosmosDbAccountName `
                        -Name $cosmosDbDatabaseName `
                        -AutoscaleMaxThroughput ${{ parameters.cosmosAutoscaleMaxThroughput }} `
                        -ErrorAction Stop
                }
                Write-Host "Cosmos DB database '$cosmosDbDatabaseName' created successfully."
            } else {
                Write-Host "Cosmos DB database '$cosmosDbDatabaseName' already exists."
            }

            # Add DevOps MI permission to Cosmos database (Data Contributor role)
            Write-Host "Adding DevOps MI permission to Cosmos database..."
            $account = Get-AzContext
            $principalId = Invoke-WithRetry -OperationName "Get Service Principal" -ScriptBlock {
                (Get-AzADServicePrincipal -ApplicationId $account.Account.Id -ErrorAction Stop).Id
            }

            Invoke-WithRetry -OperationName "CosmosDB Role Assignment" -ScriptBlock {
                New-AzCosmosDBSqlRoleAssignment `
                    -AccountName $cosmosDbAccountName `
                    -ResourceGroupName $resourceGroupName `
                    -Scope "/" `
                    -PrincipalId $principalId `
                    -RoleDefinitionId "00000000-0000-0000-0000-000000000002" `
                    -ErrorAction Stop
            }
            Write-Host "DevOps MI Cosmos DB role assignment completed."

            $templateFile = "$(System.DefaultWorkingDirectory)/samples/templates/aca/fhir-cosmos.bicep"
        }

        $templateParameters = @{
            containerAppName = $webAppName
            containerAppsEnvironmentName = $acaEnvironmentName
            keyVaultName = "${{ parameters.keyVaultName }}".ToLowerInvariant()
            fhirVersion = "${{ parameters.version }}"
            registryName = '$(azureContainerRegistry)'
            imageTag = "${{ parameters.imageTag }}"
            securityAuthenticationAuthority = "https://sts.windows.net/$(tenant-id-guid)"
            securityAuthenticationAudience = "${{ parameters.testEnvironmentUrl }}"
            enableExport = $true
            enableImport = $true
            enableConvertData = $true
            enableReindex = if ("${{ parameters.reindexEnabled }}" -eq "true") { $true } else { $false }
            minReplicas = ${{ parameters.minReplicas }}
            maxReplicas = ${{ parameters.maxReplicas }}
            containerCpu = '${{ parameters.containerCpu }}'
            containerMemory = '${{ parameters.containerMemory }}'
            acrPullUserAssignedManagedIdentityResourceId = '/subscriptions/a1766500-6fd5-4f5c-8515-607798271014/resourceGroups/HealthPlatformRG/providers/Microsoft.ManagedIdentity/userAssignedIdentities/acr-pull-uami'
            additionalEnvVars = $additionalEnvVars
        }

        if ($dataStore -eq 'sql') {
            $templateParameters["sqlServerName"] = $sqlServerName
            $templateParameters["sqlSchemaAutomaticUpdatesEnabled"] = "${{ parameters.schemaAutomaticUpdatesEnabled }}"
        } else {
            $templateParameters["cosmosDbAccountName"] = $cosmosDbAccountName
        }

        $deploymentName = "$webAppName"

        Write-Host "Provisioning ACA resources"
        Write-Host "ResourceGroupName: $resourceGroupName"
        Write-Host "ContainerAppName: $webAppName"
        Write-Host "ContainerAppsEnvironmentName: $acaEnvironmentName"

        $deploymentResult = Invoke-WithRetry -OperationName "ACA Deployment" -ScriptBlock {
            New-AzResourceGroupDeployment -Name $deploymentName -ResourceGroupName $resourceGroupName -TemplateFile $templateFile -TemplateParameterObject $templateParameters -Verbose -ErrorAction Stop
        }

        # --- Data-store-specific post-deploy setup ---
        if ($dataStore -eq 'sql') {
            $managedEnvironment = Get-AzResource -ResourceGroupName $resourceGroupName -ResourceType "Microsoft.App/managedEnvironments" -Name $acaEnvironmentName -ApiVersion "2023-05-01" -ExpandProperties
            if ($null -eq $managedEnvironment) {
                throw "Container Apps environment '$acaEnvironmentName' was not found in resource group '$resourceGroupName'."
            }

            $managedEnvironmentData = $managedEnvironment | ConvertTo-Json -Depth 100 | ConvertFrom-Json
            $outboundIps = @()

            if ($null -ne $managedEnvironmentData.Properties.outboundIpAddresses) {
                if ($managedEnvironmentData.Properties.outboundIpAddresses -is [string]) {
                    $outboundIps += ($managedEnvironmentData.Properties.outboundIpAddresses -split ',')
                } else {
                    foreach ($ip in $managedEnvironmentData.Properties.outboundIpAddresses) {
                        $outboundIps += [string]$ip
                    }
                }
            }

            if (-not [string]::IsNullOrWhiteSpace([string]$managedEnvironmentData.Properties.staticIp)) {
                $outboundIps += [string]$managedEnvironmentData.Properties.staticIp
            }

            $outboundIps = $outboundIps | ForEach-Object { $_.Trim() } | Where-Object { -not [string]::IsNullOrWhiteSpace($_) } | Sort-Object -Unique

            # Ensure Azure services firewall access is enabled for SQL.
            $allowAzureServicesRule = Get-AzSqlServerFirewallRule -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -FirewallRuleName "AllowAzureServices" -ErrorAction SilentlyContinue
            if ($null -ne $allowAzureServicesRule) {
                if ($allowAzureServicesRule.StartIpAddress -eq "0.0.0.0" -and $allowAzureServicesRule.EndIpAddress -eq "0.0.0.0") {
                    Write-Host "SQL firewall rule 'AllowAzureServices' already exists with expected address range."
                } else {
                    Invoke-WithRetry -OperationName "SQL Firewall Rule Update AllowAzureServices" -ScriptBlock {
                        Set-AzSqlServerFirewallRule -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -FirewallRuleName "AllowAzureServices" -StartIpAddress "0.0.0.0" -EndIpAddress "0.0.0.0" -ErrorAction Stop
                    }
                }
            } else {
                Invoke-WithRetry -OperationName "SQL Firewall Rule Create AllowAzureServices" -ScriptBlock {
                    New-AzSqlServerFirewallRule -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -FirewallRuleName "AllowAzureServices" -StartIPAddress "0.0.0.0" -EndIPAddress "0.0.0.0" -ErrorAction Stop
                }
            }

            if ($outboundIps.Count -eq 0) {
                Write-Warning "ACA outbound IP addresses were not found. Continuing with AllowAzureServices firewall rule only."
            } else {
                $ruleIndex = 0
                foreach ($ip in $outboundIps) {
                    $ruleIndex++
                    $ruleName = "AcaOutbound-$ruleIndex"
                    $existingRule = Get-AzSqlServerFirewallRule -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -FirewallRuleName $ruleName -ErrorAction SilentlyContinue
                    if ($null -ne $existingRule -and $existingRule.StartIpAddress -eq $ip -and $existingRule.EndIpAddress -eq $ip) {
                        Write-Host "SQL firewall rule '$ruleName' already exists for $ip."
                        continue
                    }

                    if ($null -ne $existingRule) {
                        Invoke-WithRetry -OperationName "SQL Firewall Rule Update $ruleName" -ScriptBlock {
                            Set-AzSqlServerFirewallRule -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -FirewallRuleName $ruleName -StartIpAddress $ip -EndIpAddress $ip -ErrorAction Stop
                        }
                    } else {
                        Invoke-WithRetry -OperationName "SQL Firewall Rule Create $ruleName" -ScriptBlock {
                            New-AzSqlServerFirewallRule -ResourceGroupName $resourceGroupName -ServerName $sqlServerName -FirewallRuleName $ruleName -StartIPAddress $ip -EndIPAddress $ip -ErrorAction Stop
                        }
                    }
                }
            }
        } else {
            # Assign Cosmos DB Data Contributor role to the container app's system-assigned identity
            $containerAppPrincipalId = (Get-AzResource -ResourceGroupName $resourceGroupName -ResourceType "Microsoft.App/containerApps" -Name $webAppName -ApiVersion "2023-05-01" -ExpandProperties).Identity.PrincipalId
            if (-not [string]::IsNullOrWhiteSpace($containerAppPrincipalId)) {
                Write-Host "Assigning Cosmos DB Data Contributor role to container app identity ($containerAppPrincipalId)..."
                Invoke-WithRetry -OperationName "CosmosDB Role Assignment for Container App" -ScriptBlock {
                    New-AzCosmosDBSqlRoleAssignment `
                        -AccountName $cosmosDbAccountName `
                        -ResourceGroupName $resourceGroupName `
                        -Scope "/" `
                        -PrincipalId $containerAppPrincipalId `
                        -RoleDefinitionId "00000000-0000-0000-0000-000000000002" `
                        -ErrorAction Stop
                }
                Write-Host "Container app Cosmos DB role assignment completed."

                # Assign DocumentDB Account Contributor ARM role so ResourceManagerCollectionSetup
                # can perform control plane operations (read/create databases and collections).
                $cosmosDbResourceId = (Get-AzCosmosDBAccount -ResourceGroupName $resourceGroupName -Name $cosmosDbAccountName).Id
                Write-Host "Assigning DocumentDB Account Contributor ARM role on Cosmos DB account..."
                $existingArmRole = Get-AzRoleAssignment -ObjectId $containerAppPrincipalId -RoleDefinitionName "DocumentDB Account Contributor" -Scope $cosmosDbResourceId -ErrorAction SilentlyContinue
                if ($null -eq $existingArmRole) {
                    Invoke-WithRetry -OperationName "ARM Role Assignment for Container App" -ScriptBlock {
                        New-AzRoleAssignment `
                            -ObjectId $containerAppPrincipalId `
                            -RoleDefinitionName "DocumentDB Account Contributor" `
                            -Scope $cosmosDbResourceId `
                            -ErrorAction Stop
                    }
                    Write-Host "DocumentDB Account Contributor ARM role assigned."
                } else {
                    Write-Host "DocumentDB Account Contributor ARM role already assigned."
                }
            } else {
                Write-Warning "Could not retrieve container app system-assigned identity principal ID."
            }

            # Associate Cosmos DB with Network Security Perimeter
            $nspName = "nsp-$resourceGroupName"
            $associationName = "cosmosdb-$webAppName-association"

            Write-Host "Associating CosmosDB with Network Security Perimeter: $nspName"
            $nspTemplateParameters = @{
                'nspName' = $nspName
                'resourceName' = $cosmosDbAccountName
                'resourceType' = 'Microsoft.DocumentDB/databaseAccounts'
                'associationName' = $associationName
                'accessMode' = 'Learning'
                'nspProfile' = 'default'
            }

            Invoke-WithRetry -OperationName "NSP Association Deployment" -ScriptBlock {
                New-AzResourceGroupDeployment `
                    -ResourceGroupName $resourceGroupName `
                    -Name "$webAppName-cosmosdb-nsp-association" `
                    -TemplateFile $(System.DefaultWorkingDirectory)/samples/templates/nsp-resource-association.json `
                    -TemplateParameterObject $nspTemplateParameters `
                    -Verbose `
                    -ErrorAction Stop
            }
            Write-Host "Successfully associated CosmosDB with NSP."
        }

        $containerAppUrl = $deploymentResult.Outputs.containerAppUrl.Value
        if ([string]::IsNullOrWhiteSpace($containerAppUrl)) {
            throw "ACA deployment output did not contain containerAppUrl"
        }

        $containerAppUrl = $containerAppUrl.TrimEnd('/')
        $healthCheckUrl = "$containerAppUrl/health/check"

        $timeoutMinutes = 7
        $consecutiveSuccessRequired = 5
        $consecutiveSuccessCount = 0
        $startTime = Get-Date
        $timeoutTime = $startTime.AddMinutes($timeoutMinutes)

        Write-Host "Starting health check for $healthCheckUrl"
        Write-Host "Timeout: $timeoutMinutes minutes, consecutive successes required: $consecutiveSuccessRequired"

        Do {
          Start-Sleep -s 5
          Write-Host "Checking: $healthCheckUrl (consecutive successes: $consecutiveSuccessCount/$consecutiveSuccessRequired)"
          try {
            $response = Invoke-WebRequest -Uri $healthCheckUrl -UseBasicParsing
            if ($response.StatusCode -eq 200) {
              $consecutiveSuccessCount++
              Write-Host "Success ($consecutiveSuccessCount/$consecutiveSuccessRequired)"
            }
            else {
              Write-Host "Non-200 response ($($response.StatusCode)), resetting count"
              $consecutiveSuccessCount = 0
            }
          }
          catch {
            Write-Host $_.Exception.Message
            $consecutiveSuccessCount = 0
          }
          finally {
            $Error.Clear()
          }

          if ((Get-Date) -gt $timeoutTime) {
            $elapsed = [math]::Round(((Get-Date) - $startTime).TotalMinutes, 2)
            Write-Error "Health check timed out after $elapsed minutes ($consecutiveSuccessCount/$consecutiveSuccessRequired consecutive successes)"
            throw "ACA health check timed out"
          }
        } While ($consecutiveSuccessCount -lt $consecutiveSuccessRequired)

        $elapsed = [math]::Round(((Get-Date) - $startTime).TotalSeconds, 2)
        Write-Host "ACA health check passed with $consecutiveSuccessRequired consecutive successes after $elapsed seconds"

        if ($dataStore -eq 'sql') {
            Write-Host "##vso[task.setvariable variable=TestEnvironmentUrl_${{ parameters.version }}_Sql;isOutput=true]$containerAppUrl"
            Write-Host "##vso[task.setvariable variable=TestEnvironmentUrl_Sql;isOutput=true]$containerAppUrl"
        } else {
            Write-Host "##vso[task.setvariable variable=TestEnvironmentUrl_${{ parameters.version }};isOutput=true]$containerAppUrl"
            Write-Host "##vso[task.setvariable variable=TestEnvironmentUrl;isOutput=true]$containerAppUrl"
        }
