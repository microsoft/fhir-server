name: Inferno Test

trigger:
  branches:
    include:
    - main
  paths:
    include:
    - build/inferno

pool:
  vmImage: ubuntu-latest

steps:

- script: echo "Place pipeline here"
  displayName: 'Stub'